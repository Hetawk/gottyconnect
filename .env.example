# GoTTY Configuration Template
# Copy this to .env and fill in your values

# Domain Configuration
GOTTY_DOMAIN=your-domain.com
GOTTY_WS_URL=wss://your-domain.com/ws

# Email for SSL certificate (Let's Encrypt)
SSL_EMAIL=your-email@domain.com

# Port (will auto-detect if in use)
GOTTY_PORT=7680

# Authentication Token (generate with: openssl rand -hex 32)
GOTTY_AUTH_TOKEN=generate_your_token_here

# Basic Auth Credentials (username:password)
GOTTY_CREDENTIAL=username:password
